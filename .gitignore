node_modules/
.next/
__pycache__/
*.pyc
.env
.venv/
venv/
# Raw NSE payloads: large, timestamped, regenerable via tools/capture_nse_chain.py.
# The comment must sit on its own line — git treats a trailing "# ..." as part of the
# pattern, so the version with an inline comment silently ignored nothing and 632KB of
# captures got committed.
backend/tools/nse_captures/
